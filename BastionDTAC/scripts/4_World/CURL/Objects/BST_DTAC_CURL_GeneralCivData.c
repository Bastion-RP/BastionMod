class BST_DTAC_CURL_GeneralCivData {
    private string id, member_id, active, first_name, last_name, citizen_class, date_of_birth, sex, race, alias, build, eyes, weight, height, occupation, affiliation, mental, family, hair, birthplace, nationality;

    string GetId() { return id; }
    string GetMemberId() { return member_id; }
    string GetActive() { return active; }
    string GetFirstName() { return first_name; }
    string GetLastName() { return last_name; }
    string GetCitizenClass() { return citizen_class; }
    string GetDOB() { return date_of_birth; }
    string GetSex() { return sex; }
    string GetRace() { return race; }
    string GetAlias() { return alias; }
    string GetBuild() { return build; }
    string GetEyes() { return eyes; }
    string GetWeight() { return weight; }
    string GetHeight() { return height; }
    string GetOccupation() { return occupation; }
    string GetAffiliation() { return affiliation; }
    string GetMental() { return mental; }
    string GetFamily() { return family; }
    string GetHair() { return hair; }
    string GetBirthplace() { return birthplace; }
    string GetNationality() { return nationality; }
    // Lmfao can't tostring this because dayz fucking sucks
    //string NewToString() { return "id:" + id + ", member_id:" + member_id + ", active:" + active + ", first_name:" + first_name + ", last_name:" + last_name + ", citizen_class:" + citizen_class + ", date_of_birth:" + date_of_birth + ", sex:" + sex + ", race:" + race + ", alias:" + alias + ", build:" + build + ", eyes:" + eyes + ", weight:" + weight + ", height:" + height + ", occupation:" + occupation + ", affiliation:" + affiliation + ", mental:" + mental + ", family:" + family + ", hair:" + hair + ", birthplace:" + birthplace + ", nationality:" + nationality; }
}